--
--make a dropdown that changes value for a variable for teleports
--Leechy yt inf spins update for gui

--example
--dropdown
blablabla, function(area)
  Tp = area
 end



if variable = forge then
  game.Player.LocalPlayer.HumanoidRootpart.CFrame = CFrame.new(forge)
end,

if variable = Lavender Respite
  game.Player.LocalPlayer.HumanoidRootPart.CFrame = CFrame.new(Lavender Respite)
 end,
